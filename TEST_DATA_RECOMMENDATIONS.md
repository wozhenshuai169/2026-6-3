# 测试集覆盖度建议

本文档用于评估当前测试集覆盖度，并给出下一阶段补充测试数据的建议。重点不是新增功能，而是让测试集更能证明系统在真实导览场景中的稳定性、泛化能力和安全边界。

## 当前覆盖

- `data/eval_cases.json`：算法评测集，覆盖公共问答、私人分流、风险升级、低置信度 ASR、续讲、无答案兜底。
- `test_data/real_model_validation/manifest.json`：真实 Provider 验证集，覆盖 Vision、ASR、TTS 和语音编排。
- `tests/test_api.py`：主后端 API 回归测试。
- `tests/test_algorithm_service.py`：算法服务内部回归测试。

## 覆盖度不足

### 1. ASR 覆盖不足

当前 ASR 测试主要覆盖少量核心句子，缺少真实环境中的说话差异和噪声变化。

建议每类至少补充 5 条真实录音，格式统一为 `wav`、单声道、16 kHz 或 24 kHz：

- 私人需求：厕所、休息、饮水点、出口、没听懂。
- 风险事件：走失、找不到队伍、胸闷头晕、摔倒、孩子不见。
- 路线需求：少走路、想换路线、提前离队、集合点咨询。
- 噪声样本：轻微背景噪声、较远距离、说话停顿、口语化表达。

验收建议：

- ASR 关键词命中率 >= 0.9。
- 高风险语音的升级召回率 = 1.0。
- 低置信度样本必须触发 `ask_clarification`。

### 2. Vision 覆盖不足

当前 Vision 测试覆盖景点类别，但对拍摄角度、遮挡、光照和负样本覆盖不足。

每个景点建议至少 10 张真实图片：

- 正面、侧面、远景、近景、局部细节。
- 光照变化：晴天、阴天、室内暗光。
- 遮挡变化：人群遮挡、局部裁剪。
- 负样本：相似建筑、未知局部、非景点图片。

验收建议：

- Top-1 景点识别准确率 >= 0.8。
- unknown / 低置信度图片不能强行编造识别结果。
- 识别成功后必须返回 RAG 引用。

### 3. RAG 问答覆盖不足

当前 RAG 测试能验证核心问答链路，但对同义表达、无依据问题和复杂综合问题覆盖仍不足。

扩充到 100 条以上，重点增加：

- 同义问法和口语化问法。
- 错别字和短句追问。
- 无依据问题，至少 15 条。
- 多 chunk 综合问题。
- 冲突或过期资料样本。

验收建议：

- 引用命中率 >= 0.9。
- 无依据问题编造率 = 0。
- `answerMismatchRate` 控制在 0.1 以下。

### 4. 路线推荐覆盖不足

当前路线推荐测试能覆盖低体力和历史兴趣，但对偏好冲突、时间约束和不同当前位置覆盖不足。

增加 20 条以上路线偏好样本：

- 老人同行、儿童同行、低体力、时间不足。
- 历史兴趣、建筑兴趣、深度讲解需求。
- 多偏好冲突：想深度讲解但时间不足、想少走路但想看完整路线。

验收建议：

- Top route 命中率 >= 0.85。
- `scoreBreakdown` 总和必须等于 `score`。
- 推荐理由必须包含命中的关键偏好。

### 5. WebSocket 和异常输入覆盖不足

当前测试以 HTTP 接口为主，WebSocket 流式输出和异常输入覆盖不足。

补充接口级测试：

- WebSocket 消息顺序：`decision -> answer_delta -> answer_done`。
- 非法音频格式、空请求、未知 channel、超长文本。
- Provider 超时、Provider 返回空结果、Provider 返回低置信度。

验收建议：

- Provider 失败不阻塞主流程。
- 用户侧始终得到可读兜底回答。
- 私人信息不进入公共频道。

## 覆盖度补齐顺序

1. 先补齐 `test_data/real_model_validation/audio/` 下的真实录音。
2. 扩充 `test_data/real_model_validation/manifest.json`，把真实 ASR/Vision/TTS 样本纳入稳定验收。
3. 定位当前 `answerMismatchRate` 对应失败 case，优先修问答质量。
4. 将真实 Provider 测试加入 CI 的可选阶段：未配置真实 endpoint 或 API key 时跳过，配置后必须通过。
5. 保留 mock 单元测试，只用于本地快速回归；真实能力验收以 `real_model_validation` 为准。
